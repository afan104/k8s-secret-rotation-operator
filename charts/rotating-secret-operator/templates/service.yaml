apiVersion: v1
kind: Service
metadata:
  name: {{ include "rotating-secret-operator.fullname" . }}
  labels:
    {{- include "rotating-secret-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rotating-secret-operator.selectorLabels" . | nindent 4 }}
